# pkg/apierrors

Типизированные HTTP-ошибки для использования в `transport/` и `service/`.

`service/` возвращает `*APIError` → `transport/http` читает `Code` и пишет правильный статус.

Добавляй новые ошибки сюда — не разбрасывай статус-коды по хэндлерам.
