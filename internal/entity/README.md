# internal/entity

Доменные структуры — чистые Go-structs без методов, тегов БД и HTTP.

`User` — пользователь системы.  
`Role` — типизированная строка (`user`, `admin`).

Эти структуры ходят между слоями. Не добавляй сюда логику — она живёт в `service`.
