# cmd

Точки входа — по одной директории на бинарь.

| Бинарь | Назначение |
|--------|-----------|
| `cmd/` | Основной сервер |
| `cmd/migrate/` | Применение миграций БД |

Каждая директория содержит только `main.go`. Логика здесь не живёт — только инициализация конфига и вызов `app`.
